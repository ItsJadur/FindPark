<h1> Welcome </h1>
<h2> This is where you can find reports that people have had with parks. </h2>

<h3>Submit a Report</h3>
<form action="/submit-report" method="POST">
	<label for="parkName">Park Name:</label>
	<input type="text" id="parkName" name="parkName" required />

	<label for="reportContent">Report:</label>
	<textarea
		id="reportContent"
		name="reportContent"
		rows="4"
		cols="50"
		required
	></textarea>

	<button type="submit">Submit</button>
</form>

<h3>Search and Filter Reports</h3>
<form action="/reports" method="GET">
	<label for="searchPark">Search by Park Name:</label>
	<input type="text" id="searchPark" name="searchPark" value="{{searchPark}}" />

	<label for="sortBy">Sort By:</label>
	<select id="sortBy" name="sortBy">
		<option value="date_desc" {{#if (eq sortBy "date_desc")}}selected{{/if}}>Date (Newest First)</option>
		<option value="date_asc" {{#if (eq sortBy "date_asc")}}selected{{/if}}>Date (Oldest First)</option>
		<option value="parkName_asc" {{#if (eq sortBy "parkName_asc")}}selected{{/if}}>Park Name (A-Z)</option>
		<option value="parkName_desc" {{#if (eq sortBy "parkName_desc")}}selected{{/if}}>Park Name (Z-A)</option>
	</select>

	<label for="filterByDate">Filter By Date:</label>
	<input type="date" id="filterByDate" name="filterByDate" value="{{filterByDate}}" />
	
	<button type="submit">Apply</button>
</form>

<h3>Reports</h3>
<ul>
	{{#if reports.length}}
		{{#each reports}}
			<li>
				<strong>{{parkName}}</strong>:
				{{reportContent}}
				<em>({{formatDate date}})</em>
			</li>
		{{/each}}
	{{else}}
		<li>No reports available</li>
	{{/if}}
</ul>

<a href="/" class="btn btn-primary">Home</a>